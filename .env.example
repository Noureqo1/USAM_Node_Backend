# Environment Variables for Skills & Ideas API

# JWT Secret Key - Replace with a strong, random secret key
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_replace_this_with_a_strong_random_key

# Server Port (optional, defaults to 8080)
PORT=8080
